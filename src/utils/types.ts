export interface Hole {
  holeNumber: number;
  handicap: number;
}

export interface Course {
  id?: number;
  name: string;
  // slope: number;
  // rating: number;
  // coursePar: number;
  // holes: Hole[];
}
